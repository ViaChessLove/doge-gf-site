import styled from "styled-components";
import { Container } from "../../globalStyles";


export const VaultsContainer = styled(Container)`
    height: 100vh;
    margin-bottom: clamp(100px, 110px, 120px);
    ${Container};
`;